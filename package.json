{
  "name": "@predy/v5-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "test": "graph test",
    "build": "graph build",
    "fmt:fix": "prettier --write src/**/*",
    "prepare:arbitrum": "mustache config/arbitrum.json subgraph.template.yaml > subgraph.yaml && mustache config/arbitrum.json ./src/contracts.template.ts > ./src/contracts.ts",
    "deploy:studio:arbitrum": "graph deploy --studio predy-v6-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.55.0",
    "@graphprotocol/graph-ts": "0.29.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0",
    "mustache": "^4.0.1",
    "prettier": "^2.5.1"
  }
}